# kyou-nani（今日なに？献立カレンダー）

## 作業方針
- プランはPlanエージェント（別セッション）ではなく、現在のセッション内でテキストとして提案すること
- 実装完了時は `/code-review`（low〜medium）で差分を確認してから報告する
- UI（見た目・操作・タブ切替・レイアウト等）に関わる変更のときは `/run` または `/verify` で動作確認してから報告する
